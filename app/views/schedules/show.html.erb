<div>
	Teacher: <%= @schedule.teacher_id %>
	Class: <%= @schedule.lecture_session_id %>
	Frequency: <%= @schedule.frequency %>
	Start Time: <%= @schedule.start_time %>
	Interval: <%= @schedule.time_interval %>
</div>

<div>
	<%= link_to "Update schedule record", edit_schedule_path %>
	<%= link_to "Delete schedule record", @schedule, method: :delete, confirm: "Are you sure you want to delete?" %>
</div>

<%= link_to "<< Back to index", schedules_path %>